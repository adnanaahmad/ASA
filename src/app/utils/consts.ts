/**
 * Contain all the string enumerations related to the files
 */
export enum FileEnums{
  File = 'file',
  PDFFileExt = 'pdf',
  WordFileExt = 'docx',
  ExcelFileExt = 'xlsx',
  CsvFileExt = 'csv',
  MultipleFile = 'multipleFile',
  UploadFilesExtension = 'image/*,.JPEG,.jpeg, .JPG,.jpg,.png,.PNG,.GIF,.gif ,.pdf,.ppt,.pptx,.doc,.docx,.docm,.rtf,.xls,.xlsx,.mp3,.mpg,.mpeg,.mp4,.wav,.odt ,.odp, .ods ,.tif ,.tiff, .txt , .bmp, .csv, .ccd, .ccr',
  UploadFilesTooltip = 'Select file format like docx, .pdf. xlsx,.ppt,.jpg,.mp3, .wav,.odt ,.odp, .ods ,.tif ,.tiff, .txt , .bmp, .csv, .ccd, .ccr',
  MediaPath = 'assets/media/',
  AliviaLogoImg = 'logo.alivia.png',
}

/**
 * contains all the string within the app
 */
export enum UIStrings {
  Alivia = 'Alivia Analytics'
}
